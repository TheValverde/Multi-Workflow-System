# fs-agent LangGraph Agent

This README exists so the Hatchling build backend has documentation to bundle
when `langgraph-cli` installs the agent in editable mode. It mirrors the root
project and doesn't need any additional content right now.


